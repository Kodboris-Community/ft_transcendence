version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - SERVER_NAME=${SERVER_NAME}
    ports:
      - "443:443"
    networks:
      - frontend

networks:
  frontend:
    driver: bridge
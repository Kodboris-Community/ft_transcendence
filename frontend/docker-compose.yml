version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      - SERVER_NAME=${SERVER_NAME}
    volumes:
      - ./frontend:/usr/share/nginx/html
    ports:
      - "443:443"
    networks:
      - frontend

networks:
  frontend:
    driver: bridge
